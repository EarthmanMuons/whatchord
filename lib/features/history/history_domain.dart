// Pure-Dart seam for the history feature, mirroring theory_domain.dart: model
// types only, no Riverpod or Flutter imports, so CLI tooling and the WhatKey
// harness can compile against ChordEvent without pulling in the app.
export 'models/chord_event.dart';
